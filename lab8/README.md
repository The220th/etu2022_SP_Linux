# Компиляция и запуск

``` bash
> g++ main.cpp -o main

# Следующие 3 строки запускать в любом порядке. {queue_key} - это положительно число, одинаковое для всех.
> ./main 0 {queue_key}
> ./main 1 {queue_key}
> ./main 2 {queue_key}
```